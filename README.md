# Daytona Benchmark

This repository contains benchmarks for evaluating AI models on **Daytona**,
an open-source development environment manager that automates the creation of fully configured development environments on any infrastructure.

You can view the evaluation reports at [https://kweizh.github.io/daytona-benchmark/](https://kweizh.github.io/daytona-benchmark/).

## What is Daytona

[Daytona](https://www.daytona.io/) is an open-source development environment manager.
It provides a single command to spin up fully configured, standardized development environments.
Key features include:
- **Standardized Environments**: Define your environment once with devcontainer.json, and run it anywhere.
- **Any Infrastructure**: Run workspaces locally, on your own servers, or in the cloud.
- **IDE Integration**: Seamlessly connect your favorite IDEs (VS Code, JetBrains, Cursor, etc.).
- **Daytona CLI & SDKs**: First-class tooling via the `daytona` CLI and programmatic interfaces for Python and TypeScript to manage workspaces, providers, and profiles.

## About this Leaderboard

This leaderboard is generated by [Zealt](https://github.com/TabbyML/zealt)
to evaluate how effectively different AI agents and Large Language Models (LLMs) can utilize the Daytona CLI and SDKs.

As AI coding assistants become more prevalent, it's crucial to understand their proficiency with modern development environment managers.
This benchmark provides a standardized environment to measure an agent's capability in handling end-to-end Daytona development tasks,
including
- workspace creation and lifecycle management
- provider configuration
- profile management
- devcontainer configuration
- CLI operations
- SDK programmatic control
and others.

## How to Contribute

We welcome contributions from the community to make this benchmark more comprehensive! Here are a few ways you can help:

### 1. Add New Benchmark Tasks

You can create new evaluation scenarios in the `tasks/` directory. Good tasks should test specific aspects of Daytona, such as
workspace configuration, provider setup, profile management, or complex CLI/SDK workflows.

Each task is defined in its own directory and must follow a specific structure compatible with the Harbor framework:
- `instruction.md`: The prompt and instructions provided to the AI agent.
- `task.toml`: Configuration for the task (metadata, timeouts, etc.).
- `environment/Dockerfile`: The Docker environment definition where the task and evaluations are executed.
- `bootstrap/`: Contains files to set up the initial state before the agent starts (e.g., `task.json`, `test_initial_state.py`).
- `tests/`: Contains the evaluation scripts to verify the agent's solution (e.g., `test.sh`, `test_final_state.py`).

### 2. Submit New Evaluation Results

Help us expand the leaderboard by running evaluations for new models or agents and submitting the results in the `jobs/` directory.
This benchmark is evaluated using the [Harbor framework](https://github.com/harbor-framework/harbor). You can run the evaluation using the Harbor CLI:

```bash
harbor run \
  --agent codex \
  --model "gpt-5.2-codex" \
  --env daytona \
  --path ./tasks \
  --n-attempts 1 \
  --max-retries 5 \
  --n-concurrent 5 \
  --retry-include RuntimeError \
  --retry-include DaytonaError \
  --retry-include AgentTimeoutError
```

*Note: Before starting, ensure you have set the necessary environment variables for your chosen agent. Evaluations can be run locally with Docker (default) or using [Daytona.io](https://www.daytona.io/).*

### 3. Improve the Visualization Site

The leaderboard visualization is a Next.js application located in the `site/` directory. Contributions to improve the UI, add new metrics, or enhance the data presentation are highly appreciated.

---
Generated by [Zealt](https://github.com/TabbyML/zealt)
